# ===============================
# development.env Configuration File
# ===============================
#
# NOTE: This file is intended for non-sensitive configuration settings only.
# DO NOT include sensitive information (e.g., passwords, API keys, secrets).
#
# This file contains configurations specific to the development environment.
# Shared configurations with production should be placed in app.env.
#
# For sensitive configurations, please create a separate file (e.g., secrets.env)
# and ensure it is excluded from version control. All other .env files in this
# directory are automatically included in .gitignore to prevent accidental 
# exposure of sensitive data.
#
# Example sensitive config file: secrets.env
#
# ===============================

# Environment mode (should always be 'development' for this file)
NODE_ENV=development

# Port on which the application runs
PORT=3001

# Frontend origin for CORS (update as necessary for your development environment)
CORS_ORIGIN=http://localhost:3000

# Enable or disable debug mode
DEBUG=true

# Log level for development (info for more verbose logging)
LOG_LEVEL=info

# Format for logging (e.g., json or text)
LOGGING_FORMAT=json

# Maximum time (in milliseconds) to wait for a request
REQUEST_TIMEOUT=10000
